# Long source

Some intro text that mentions [[daily/2026-05-07]] in body but NOT in a heading line. The plugin should ignore this match.

## Heading with [[daily/2026-05-07]] link

This is the section that should appear under "Heading-linked mentions" in the daily note's reading view.

It contains a few paragraphs so the rendered section has visible content.

### A subheading inside the section

Subheadings should fold inside the linked-content body per `foldSubheadings`.

## Another heading without link

Should not appear in the linked-content panel because the heading line has no backlink to the daily note.
